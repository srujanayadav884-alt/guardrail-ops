# Production environment for the frontend build.
# Vite bakes this in at BUILD time (not container runtime) — when building
# via Docker, pass it as a build arg instead:
#   docker build --build-arg VITE_API_BASE_URL=https://api.your-domain.example/api .
VITE_API_BASE_URL=https://api.your-domain.example/api
